<% if book.favorited_by?(current_user) %>
  <%= link_to book_favorite_path(book), method: :delete, remote: true do %>
    <i class="fa-solid fa-heart" style="color: #ff0a0a;"></i> <%= book.favorites.count %>
  <% end %>
<% else %>
  <%= link_to book_favorite_path(book), method: :post, remote: true do %>
    <i class="fa-solid fa-heart" style="color: #74C0FC;"></i> <%= book.favorites.count %>
  <% end %>
<% end %>